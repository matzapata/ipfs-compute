package commands

import (
	"github.com/matzapata/ipfs-compute/provider/internal/config"
)

func ApproveCommand(config *config.Config, amount string, price string, providerAddress string, adminPrivateKey string) {
	panic("Not implemented")
}
